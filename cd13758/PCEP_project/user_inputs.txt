Transformational AI: Analyzing Ecommerce Large Datasets for Machine Learning
Completed by: Rouzbeh Radparvar
—————————————-

Question 1: The reviews dataset contains 100 individual customer reviews and 10 columns. The rating column is a float; timestamp and helpful_vote are integers; verified_purchase is Boolean; and title, text, images, ASIN identifiers, and user_id are stored as object columns. Each row represents one review, pairing the written title and review text with its rating, product identifiers, purchase-verification status, timestamp, helpful votes, and any review images.
Question 2: Yes, the output is what I expected. DataFrame.head() returned the first five rows by default and preserved the same columns and data types as the full DataFrame. The wide review text and nested image values make the plain-text display dense, but it is still useful for quickly confirming that the data loaded correctly.
Question 3: I set Items_To_Print to the integer 10 and passed that value to head() so the first ten reviews were printed instead of pandas' default five. I also set display.max_columns to show every column, increased display.width to reduce line wrapping, and limited display.max_colwidth so long review text would be truncated to a readable width.
Question 4: The reviews dataset describes customer interactions: each row contains a rating, review title and text, reviewer and product identifiers, timestamp, helpful-vote count, verification flag, and review images. The metadata dataset describes the products themselves and has fields such as product title, average rating, rating count, features, description, price, store, category, product images and videos, and product details. Reviews are event-level data, while metadata is product-level data that is better suited to comparing titles, prices, and aggregate ratings.
Question 5: I found DataFrame.info() the most helpful method because it summarizes the number of rows, column names, non-null counts, data types, and memory usage in one compact output. In this project it immediately revealed which product fields had missing values and confirmed that price needed numeric conversion before filtering.
Question 6: The price distribution is strongly right-skewed. Most highly rated products in this 100-record sample cost less than about $100, with a median price of $24.63. A $909.99 security-camera system is a clear high-price outlier, so the frequency drops sharply as price increases.
Question 7: There is no strong price-to-rating relationship in this small filtered sample. Ratings from 4.5 to 5.0 occur across inexpensive and moderately priced products. The calculated correlation is moderately positive, but it is heavily influenced by one $909.99 product rated 5.0, so price alone would not be a reliable predictor of customer rating.
