# Completed project artifacts

This directory contains the four files required for the Udacity project
submission:

1. `Udacity - PCEP Project.ipynb` — completed notebook with saved outputs
2. `user_inputs.txt` — responses to all seven reflection questions
3. `top_products.csv` — cleaned product dataset in CSV format
4. `top_products.parquet` — the same cleaned dataset in Parquet format

